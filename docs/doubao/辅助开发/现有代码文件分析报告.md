# 现有8个代码文件逐行分析报告

> 供OpenCode开发时直接使用。每个文件标注：当前状态、TODO位置、具体问题、修改建议。
> 所有行号基于当前文件实际内容。

## 文件总览

| # | 文件 | 行数 | 完成度 | 核心问题 |
|---|------|------|--------|----------|
| 1 | MTEProductionLineAggregator.java | 268 | 30% | 结构验证/配方匹配/输入输出/能量全是TODO |
| 2 | MTEHatchProductionLineController.java | 222 | 40% | GUI未打开，激活逻辑未接入 |
| 3 | ProductionLineRecipe.java | 157 | 60% | 缺inputs字段，machineId格式错误 |
| 4 | ProductionLineRecipeLoader.java | 279 | 35% | JSON字段名不匹配，registerRecipe是TODO |
| 5 | ProductionLineRecipeMaps.java | 20 | 20% | maxIO严重不足（6/4/2/2） |
| 6 | ProductionLineConfig.java | 113 | 80% | 基本可用 |
| 7 | MachineConsumptionManager.java | 89 | 70% | 全局保存，应改为控制器仓NBT独立 |
| 8 | gui/ProductionLineControllerGUI.java | 184 | 25% | 用Container而非ModularUI2，设计方向可能错误 |

---

## 1. MTEProductionLineAggregator.java

**路径**：`src/main/java/com/wztwzt/ae2_qof/productionline/MTEProductionLineAggregator.java`

### 当前状态
继承`MTEMultiBlockBase`，有基本的多方块框架（输入/输出总线/仓列表、进度跟踪、NBT保存），但核心逻辑全是TODO。

### 逐行问题与修改建议

#### 第32行：`private MTEHatchProductionLineController mController;`
- **问题**：控制器仓引用有，但checkMachine()中没有实际查找和赋值逻辑
- **修改**：在checkStructure()中遍历结构方块，找到MTEHatchProductionLineController并赋值

#### 第68-84行：`checkMachine()`
- **问题**：调用checkStructure()后只检查mController!=null，没有验证输入/输出总线/能量仓
- **修改**：
  ```java
  // 需要验证：至少1个输入总线、1个输出总线、1个能量仓、1个控制器仓
  // 参考GT5 MTEDistillationTower.checkMachine()的实现模式
  ```

#### 第86-89行：`checkStructure()` — **核心TODO**
```java
private boolean checkStructure() {
    // TODO: implement expandable structure validation
    return true;
}
```
- **问题**：直接返回true，没有5×5×5立方体验证
- **修改**：实现5×5×5立方体结构验证
  - 控制器在正面中心
  - 外壳用机器外壳（等级由外壳决定：铜=LV，钢=MV，铝=HV，不锈钢=EV，钛=IV，钨钢=LuV，铱=ZPM，氖=UV，锇=UHV）
  - 内部3×3×3空气或可替换方块
  - 参考GT5 `MTEBlastFurnace.checkStructure()` 或 `MTELargeChemicalReactor.checkStructure()`
  - **关键**：外壳等级决定minVoltageTier，需要记录外壳材料类型

#### 第92-113行：`checkProcessing()` — **核心逻辑缺失**
```java
@Override
public CheckRecipeResult checkProcessing() {
    int machineVoltageTier = getMachineVoltageTier();
    if (mCurrentRecipe != null) {
        // 检查电压、机器消耗、输入
        return processRecipe(mCurrentRecipe);
    }
    return CheckRecipeResultRegistry.NO_RECIPE;
}
```
- **问题1**：`mCurrentRecipe`永远为null——没有从输入自动匹配配方的逻辑
- **问题2**：没有遍历所有已加载配方进行匹配
- **修改**：
  ```java
  // 1. 收集所有输入总线的物品和输入仓的流体
  // 2. 遍历ProductionLineRecipeLoader.getAllRecipes()
  // 3. 对每个配方：检查输入是否包含配方所需的所有物品/流体（含催化剂）
  // 4. 找到第一个匹配的配方，设置mCurrentRecipe
  // 5. 检查电压等级、机器激活状态
  // 6. 调用processRecipe()
  ```
- **催化剂匹配规则**：inputs和outputs中相同item+fluid且amount相同的物品/流体，只检查存在性，不消耗

#### 第115-118行：`checkInputs()` — TODO
```java
private boolean checkInputs(ProductionLineRecipe recipe) {
    // TODO: check input items and fluids
    return true;
}
```
- **修改**：遍历mInputBuses和mInputHatches，检查是否有足够的物品和流体
- **注意**：催化剂只检查存在，不检查数量（或检查数量>=配方中的数量但不消耗）

#### 第120-125行：`processRecipe()` — TODO
```java
private CheckRecipeResult processRecipe(ProductionLineRecipe recipe) {
    // TODO: consume inputs and produce outputs
    mMaxProgress = recipe.getDuration();
    mEUt = recipe.getEuPerTick();
    return CheckRecipeResultRegistry.SUCCESSFUL;
}
```
- **问题**：只设置了进度和能耗，没有实际消耗输入
- **修改**：
  ```java
  // 1. 消耗输入物品（从mInputBuses中decrStackSize）
  // 2. 消耗输入流体（从mInputHatches中drain）
  // 3. 催化剂不消耗
  // 4. 设置mMaxProgress和mEUt
  // 5. 返回SUCCESSFUL
  ```

#### 第127-130行：`getMachineVoltageTier()` — 返回0
```java
private int getMachineVoltageTier() {
    // Get voltage tier from the structure
    return 0;
}
```
- **修改**：从checkStructure()中记录的外壳材料等级返回
  - 铜外壳=1(LV), 钢=2(MV), 铝=3(HV), 不锈钢=4(EV), 钛=5(IV), 钨钢=6(LuV), 铱=7(ZPM), 氖=8(UV), 锇=9(UHV)

#### 第133-144行：`onPostTick()`
- **问题**：进度递增逻辑有，但没有能量消耗检查（mEUt设置了但没有实际扣能量）
- **修改**：需要在onPostTick中调用`getBaseMetaTileEntity().decreaseStoredEnergyUnits(mEUt, false)`或类似方法
- **参考**：GT5 `MTEMultiBlockBase.onPostTick()` 的能量处理模式

#### 第146-148行：`completeProcessing()` — TODO
```java
private void completeProcessing() {
    // TODO: output products
}
```
- **修改**：
  ```java
  // 1. 将配方outputs中的物品添加到mOutputBuses
  // 2. 将配方outputs中的流体填充到mOutputHatches
  // 3. 如果输出总线/仓满了，等待（不清除进度，直到有空间）
  // 4. 重置mCurrentRecipe=null（允许下一次匹配新配方）
  ```

#### 第151-171行：NBT保存/加载
- **问题**：保存了CurrentRecipe/Progress/MaxProgress/EUt，但没有保存外壳等级、已激活配方列表
- **修改**：增加外壳等级字段的NBT保存

#### 第197-267行：IInventory接口方法
- **问题**：全部返回空/false，这意味着多方块本身没有物品槽（正确，因为用总线/仓）
- **状态**：基本正确，不需要改

### 缺失的关键方法
- `addRecipe()` / `checkRecipe()` — GT5多方块标准方法，需要正确实现以接入RecipeMap系统
- `getRecipeMap()` — 返回ProductionLineRecipeMaps.productionLineRecipes
- `getMaxRecipes()` / `getRecipeByIndex()` — NEI显示需要
- `isInputBus()` / `isOutputBus()` / `isInputHatch()` / `isOutputHatch()` — 结构验证需要

---

## 2. MTEHatchProductionLineController.java

**路径**：`src/main/java/com/wztwzt/ae2_qof/productionline/MTEHatchProductionLineController.java`

### 当前状态
继承`MTEHatch`，有NBT保存激活列表，但GUI未打开，激活逻辑未接入。

### 逐行问题与修改建议

#### 第28-29行：字段
```java
private String selectedRecipeId;
private final List<String> activatedRecipes = new ArrayList<>();
```
- **问题**：selectedRecipeId是"选择配方"的概念，但用户设计是纯GT自动匹配，不需要手动选择
- **修改**：selectedRecipeId可以保留用于GUI显示当前匹配的配方，但activatedRecipes是核心（已激活的配方列表）

#### 第74-80行：`onRightclick()` — **核心问题**
```java
public boolean onRightclick(...) {
    if (aTileEntity.isServerSide()) {
        sendMessage(aPlayer, "产线聚合器控制器 - 功能开发中");
    }
    return true;
}
```
- **问题**：只发聊天消息，没有打开GUI
- **修改**：
  ```java
  // 方案A（ModularUI2）：返回ModularUI实例
  // 方案B（原生Container）：player.openGui(mod, guiId, world, x, y, z)
  // 参考项目中其他MTEHatch的GUI打开方式（如AdaptiveNetTerminal）
  ```
- **注意**：项目中AdaptiveNetTerminal用的是ModularUI2，建议保持一致

#### 第88-109行：NBT保存/加载
- **状态**：activatedRecipes的NBT保存/加载正确
- **问题**：selectedRecipeId的保存有，但load时没有空值检查（aNBT.getString永远返回非null）

#### 第111-134行：激活方法
```java
public void selectRecipe(String recipeId) { ... }
public boolean activateRecipe(String recipeId, MachineConsumptionManager manager) { ... }
public boolean isRecipeActivated(String recipeId) { ... }
```
- **问题1**：activateRecipe调用了全局MachineConsumptionManager，但用户要求"激活存控制器仓NBT每结构独立"
- **问题2**：这些方法没有被GUI或MTEProductionLineAggregator调用
- **修改**：
  - 移除对全局MachineConsumptionManager的依赖，激活状态只存在本仓的activatedRecipes列表中
  - MTEProductionLineAggregator.checkProcessing()中通过mController.isRecipeActivated(recipeId)检查
  - GUI中调用mController.activateRecipe(recipeId)

#### 第142-149行：getDescription()
- **状态**：描述正确

### 关键设计决策
用户要求"激活存控制器仓NBT每结构独立"——这意味着：
- 每个产线聚合器结构有自己的控制器仓
- 激活状态存在控制器仓的NBT中（已实现activatedRecipes列表）
- **不需要全局MachineConsumptionManager**（第7个文件可以废弃或简化）

---

## 3. ProductionLineRecipe.java

**路径**：`src/main/java/com/wztwzt/ae2_qof/productionline/ProductionLineRecipe.java`

### 当前状态
数据结构基本完整，但缺少inputs字段，machineId格式错误。

### 逐行问题与修改建议

#### 第15-49行：字段列表
- **问题1**：缺少`itemInputs`和`fluidInputs`字段！只有outputs
- **问题2**：缺少`catalysts`字段（催化剂列表）
- **修改**：增加
  ```java
  private final List<ItemStack> itemInputs;
  private final List<FluidStack> fluidInputs;
  private final List<ItemStack> itemCatalysts;  // 输入=输出的物品
  private final List<FluidStack> fluidCatalysts; // 输入=输出的流体
  ```

#### 第51-67行：构造函数
- **修改**：增加inputs和catalysts参数

#### 第120-156行：RequiredMachine内部类
```java
public static class RequiredMachine {
    private final String machineId;  // "gtceu:distillation_tower" — 高版本格式！
    private final String machineName;
    private final int count;
    private final boolean consumed;
}
```
- **问题1**：machineId格式是`gtceu:xxx`（高版本GTCEu格式），1.7.10 GT5应该用`gregtech:gt.blockmachines`+meta值
- **问题2**：缺少meta字段
- **修改**：
  ```java
  public static class RequiredMachine {
      private final String itemId;   // "gregtech:gt.blockmachines"
      private final int meta;        // 机器meta值
      private final String machineName;
      private final int count;
  }
  // 移除consumed字段——用户设计是"机器消耗激活"，激活时消耗机器物品
  ```

#### 缺失的方法
- `getItemInputs()` / `getFluidInputs()`
- `getItemCatalysts()` / `getFluidCatalysts()`
- `matches(ItemStack[] items, FluidStack[] fluids)` — 配方匹配方法

---

## 4. ProductionLineRecipeLoader.java

**路径**：`src/main/java/com/wztwzt/ae2_qof/productionline/ProductionLineRecipeLoader.java`

### 当前状态
JSON解析框架有，但字段名与实际生成的24条配方JSON严重不匹配，registerRecipe是TODO。

### 逐行问题与修改建议

#### 第31行：`RECIPE_DIR = "config/ae2_qof/production_lines/"`
- **问题**：只从config目录加载，没有从模组资源文件夹加载
- **修改**：
  ```java
  // 优先从config/ae2_qof/production_lines/加载（用户自定义）
  // 其次从classpath:assets/ae2_qof/production_lines/加载（模组内置24条）
  // 参考GT5 RecipeMap的资源加载方式
  ```

#### 第40-66行：`loadRecipes()`
- **问题1**：只在目录不存在时创建默认配方，目录存在时不创建
- **问题2**：没有调用时机（需要在FMLInitializationEvent或postInit中调用）
- **修改**：
  - 在`@Mod.EventHandler`的init或postInit中调用`ProductionLineRecipeLoader.loadRecipes()`
  - 将24条内置JSON放到`resources/assets/ae2_qof/production_lines/`

#### 第71-137行：`loadRecipe()` — **字段名严重不匹配**

| 代码读取的字段 | 实际JSON中的字段 | 问题 |
|---------------|-----------------|------|
| `finalOutputs.items` | `outputs.items` | 不匹配！ |
| `finalOutputs.fluids` | `outputs.fluids` | 不匹配！ |
| `requiredMachines[].id` | `requiredMachines[].item` | 不匹配！ |
| `requiredMachines[].name` | `requiredMachines[].note` | 不匹配！ |
| 无 | `inputs.items` | 完全没解析！ |
| 无 | `inputs.fluids` | 完全没解析！ |
| 无 | `_note` | 非标准字段，应忽略 |

- **修改**：完全重写JSON解析逻辑，匹配实际的24条配方JSON格式：
  ```json
  {
    "type": "production_line",
    "id": "hydrogen",
    "name": "氢产线",
    "category": "lv_gas",
    "description": "...",
    "minVoltageTier": 1,
    "requiredMachines": [
      {"item": "gregtech:gt.blockmachines", "meta": 0, "count": 1, "note": "..."}
    ],
    "inputs": {
      "items": [{"item": "...", "meta": 0, "count": 1, "note": "..."}],
      "fluids": [{"fluid": "water", "amount": 1000, "note": "..."}]
    },
    "outputs": {
      "items": [...],
      "fluids": [...]
    },
    "euPerTick": 30,
    "duration": 2000,
    "parallel": 64
  }
  ```

#### 第82-92行：requiredMachines解析
```java
String machineId = machineJson.get("id").getAsString();  // 应为"item"
String machineName = machineJson.get("name").getAsString(); // 应为"note"
```
- **修改**：改为读取`item`/`meta`/`count`/`note`

#### 第94-122行：outputs解析
- **修改**：改为读取`outputs`（非`finalOutputs`），同时增加`inputs`解析

#### 第142-160行：`parseItemStack()`
- **问题**：只支持`modId:name`格式，不支持OreDict（如`ore:dustSilicon`）
- **修改**：增加OreDict支持，或直接用item+meta
- **注意**：24条配方JSON中用的是`gregtech:gt.metaitem.01`+meta格式

#### 第183-186行：`registerRecipe()` — **核心TODO**
```java
private static void registerRecipe(ProductionLineRecipe recipe) {
    // TODO: 注册到ProductionLineRecipeMaps.productionLineRecipes
    // 需要构建GTRecipe对象
}
```
- **修改**：
  ```java
  // 构建GTRecipe并注册到RecipeMap
  // 参考GT5 RecipeMap.addRecipe()的用法
  // 注意：产线聚合器的配方是"合并配方"，输入是全部原材料，输出是全部产物
  // GTRecipe的inputs/outputs直接用配方的inputs/outputs
  // 催化剂需要特殊处理（GTRecipe不支持"不消耗的输入"，需要在checkProcessing中手动处理）
  ```
- **关键问题**：GT5的RecipeMap系统不支持"催化剂"（输入但不消耗的物品）。有两种方案：
  - 方案A：催化剂不作为GTRecipe的输入，只在MTEProductionLineAggregator.checkProcessing()中额外检查存在性
  - 方案B：用GTRecipe的specialValue或NBT标记催化剂，在processRecipe中不消耗
  - **建议方案A**：更简单，不侵入GTRecipe系统

#### 第191-264行：`createDefaultRecipe()`
- **问题**：默认配方用的是高版本格式（gtceu:xxx、steps数组、finalOutputs），与实际JSON格式完全不同
- **修改**：删除此方法，改为从模组资源文件夹复制24条内置JSON到config目录（或直接从classpath加载）

---

## 5. ProductionLineRecipeMaps.java

**路径**：`src/main/java/com/wztwzt/ae2_qof/productionline/ProductionLineRecipeMaps.java`

### 当前状态
```java
public static final RecipeMap<RecipeMapBackend> productionLineRecipes = 
    RecipeMapBuilder.of("gt.recipe.production_line_aggregator")
        .maxIO(6, 4, 2, 2)  // 6物品输入/4物品输出/2流体输入/2流体输出
        .minInputs(1, 1)
        .build();
```

### 严重问题
**maxIO严重不足！** 24条配方中：
- 铂处理：12物品输入 / 13物品输出
- 稀土处理：10+物品输入 / 15+物品输出
- 下界合金：5物品输入 / 16物品输出
- 硅岩处理：8物品输入 / 10物品输出
- 凯芙拉：10+物品输入 / 8+物品输出

### 修改建议
```java
.maxIO(24, 24, 16, 16)  // 大幅提高上限
.minInputs(0, 0)  // 允许纯物品或纯流体配方
```
- **注意**：maxIO是int，最大可能受RecipeMapBackend限制，需要确认GT5的上限
- 如果maxIO不够，需要自定义RecipeMapBackend或在checkProcessing中手动处理（不走GT RecipeMap系统）

---

## 6. ProductionLineConfig.java

**路径**：`src/main/java/com/wztwzt/ae2_qof/productionline/ProductionLineConfig.java`

### 当前状态
配置框架完整，4个配置项：recipeDir/maxParallel/machineConsumptionMultiplier/debugMode。

### 问题与建议
- **基本可用**，不需要大改
- **建议增加**：
  - `enableBuiltinRecipes`（是否启用内置24条配方）
  - `requireMachineActivation`（是否需要机器消耗激活，默认true）
- **调用时机**：需要在FMLPreInitializationEvent中调用`ProductionLineConfig.loadConfig()`

---

## 7. MachineConsumptionManager.java

**路径**：`src/main/java/com/wztwzt/ae2_qof/productionline/MachineConsumptionManager.java`

### 当前状态
继承`WorldSavedData`，全局保存已消耗配方列表。

### 关键问题
用户明确要求：**"激活存控制器仓NBT每结构独立"**
- 当前设计是全局WorldSavedData（一个玩家激活后，所有结构都能用）
- 应该改为：每个MTEHatchProductionLineController自己保存activatedRecipes列表（已实现！）
- **结论**：MachineConsumptionManager可以**废弃**，激活逻辑完全由控制器仓的NBT处理

### 如果保留
- 需要改为按结构位置（dimension+x+y+z）保存，而不是全局
- 但这比直接用控制器仓NBT更复杂，建议废弃

---

## 8. gui/ProductionLineControllerGUI.java

**路径**：`src/main/java/com/wztwzt/ae2_qof/productionline/gui/ProductionLineControllerGUI.java`

### 当前状态
继承原生`Container`，6个机器槽位，有激活逻辑。

### 严重问题

#### 问题1：用Container而非ModularUI2
- 项目中AdaptiveNetTerminal用的是ModularUI2（Flow/TextWidget/IKey）
- 建议保持一致，改用ModularUI2

#### 问题2：设计方向可能错误
用户明确要求：**"纯GT风格放入自动匹配（否决点击选择GUI）"**
- 当前GUI有6个机器槽位，需要手动插入机器物品
- 但用户设计是"机器消耗激活"——在GUI中放入所需机器物品→点击激活→机器被消耗→配方解锁
- 这个设计是合理的，但需要确认：
  - 机器槽位数量：6个不够（凯芙拉需要24台机器），需要动态扩展或用滚动列表
  - 机器匹配方式：用item+meta匹配，而非getUnlocalizedName

#### 第37-49行：6个机器槽位
```java
for (int i = 0; i < 6; i++) {
    Slot slot = new Slot(null, i, x, y) { ... };
}
```
- **问题1**：`new Slot(null, ...)`第一个参数是IInventory，传null会导致NPE
- **问题2**：6个槽位不够
- **修改**：需要一个内部IInventory实现（如MachineSlotInventory），槽位数量动态根据当前配方的requiredMachines数量

#### 第86-111行：`checkRequiredMachines()`
```java
String machineId = stack.getItem().getUnlocalizedName(stack);
```
- **问题**：用getUnlocalizedName匹配，但JSON中的machineId是`gregtech:gt.blockmachines`格式
- **修改**：用`stack.getItem().getRegistryName()` + `stack.getItemDamage()`匹配

#### 第116-145行：`consumeRequiredMachines()`
- **逻辑正确**：消耗机器物品
- **问题**：没有检查机器是否匹配正确（可能消耗错误的机器）

#### 第150-169行：`activateRecipe()`
- **逻辑正确**：检查机器→消耗机器→标记激活
- **问题**：标记到全局MachineConsumptionManager，应改为标记到控制器仓的activatedRecipes

### 修改建议
1. 改用ModularUI2（参考AdaptiveNetTerminal的GUI实现）
2. 机器槽位用内部IInventory，数量动态
3. 激活状态存控制器仓NBT（不用全局Manager）
4. GUI显示：当前匹配的配方、所需机器列表、已激活状态、激活按钮

---

## 总体修改优先级

### P0（必须先做，否则无法运行）
1. **ProductionLineRecipeMaps.java**：maxIO从6/4/2/2提高到24/24/16/16
2. **ProductionLineRecipe.java**：增加inputs字段，修正RequiredMachine格式
3. **ProductionLineRecipeLoader.java**：重写JSON解析匹配实际格式，实现registerRecipe
4. **MTEProductionLineAggregator.java**：实现checkStructure（5×5×5）、配方匹配、输入输出、能量

### P1（核心功能）
5. **MTEHatchProductionLineController.java**：实现GUI打开，激活逻辑接入
6. **gui/ProductionLineControllerGUI.java**：改用ModularUI2，动态机器槽位
7. **MachineConsumptionManager.java**：废弃或改为控制器仓NBT

### P2（完善）
8. **ProductionLineConfig.java**：增加配置项，确认调用时机
9. 注册：在主mod类中注册MTE和Hatch（ID 32120/32121）
10. 本地化：en_us.lang/zh_cn.lang
11. 材质：产线聚合器和控制器仓的贴图

---

## 关键技术参考

### GT5多方块实现参考
- `MTEDistillationTower.java`：结构验证+多输出
- `MTELargeChemicalReactor.java`：大型化学反应釜
- `MTEBlastFurnace.java`：外壳等级限制
- `MTEAssemblyline.java`：装配线（机器消耗激活类似）

### GT5 RecipeMap参考
- `RecipeMaps.java`：所有标准RecipeMap定义
- `RecipeMapBuilder.java`：maxIO用法
- `GTRecipe.java`：配方数据结构

### 项目内参考
- `AdaptiveNetTerminal.java`：ModularUI2 GUI实现
- `MTEHatchAdaptiveNetwork.java`：自定义Hatch实现

---

*生成时间：2026-09-02 | 基于8个文件实际内容逐行分析*
