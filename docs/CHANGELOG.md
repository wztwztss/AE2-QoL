# AE2 QoL — 更新日志

> 面向使用者的简洁更新日志（按版本）。开发者向详细改动、已知风险登记见项目根目录的 **`CHANGELOG.md`**。

---

### 3.3.0 — 统一配置文件 + 热加载 + OP 管理命令（2026-08-16，作者 wztwzt）
- **新增**：统一配置文件 `config/ae2_qof/settings.json`，可直接修改 `io_port_rate`（强化 IO 端口倍率，默认 1024）和 `smart_doubling_max_rounds`（智能倍增最大轮数，默认 64）
- **新增**：**热加载**——直接编辑 `settings.json` 保存后约 1 秒自动生效，单机与服务端均可，无需重启
- **新增**：OP 命令 `/ae2qof reload`（立即重载配置 + 配方映射）、`/ae2qof status`（查看当前配置值）。服务端需 OP（等级 2），单机/局域网主机默认可直接用
- **改进**：旧 `config/ae2_qof.cfg` 中的数值配置首次启动自动迁移到 `settings.json` 并清理旧文件
- **配方映射** `recipe_names.json` 也可通过 `/ae2qof reload` 热重载

### 3.2.0 — 智能倍增 Smart Doubling（2026-08-16，作者 wztwzt）
- **新增**：ME 接口新增「智能倍增」复选框（接口 GUI 左侧循环箭头按钮）。勾选后，合成 CPU 对挂在接口上的样板**一次性推送多轮**材料，机器连做多轮，补料不再逐轮等待，大幅加快 GT 流水线
- **新增**：新增配置项 `smartDoublingMaxRounds`（默认 64），控制单次最多推送轮数（3.3.0 起位于 `config/ae2_qof/settings.json` 的 `smart_doubling_max_rounds`，可热加载）
- **安全**：假合成 / 流体接口 / 阻塞模式 / 机器接不下（经模拟探测）等场景自动退回单轮推送，与原版行为一致，不会超产或丢物
- **适用**：标准 GT 机器（非 `acceptsPlans`）、AE2 合成器（流体接口暂不支持多轮）

### 3.1.2 — 修复流体数量随机出现在物品上的显示 bug（2026-08-16，作者 wztwzt）
- **修复**：修复了"液体数量的显示会随机出现在一些物品上面"的问题——此前普通物品（如铁锭、木板）的 damage 值恰好命中流体注册 ID（damage=0 → 水），被误判为流体，NEI 角标与悬浮提示在随机物品上显示 `X mB 水/岩浆/…`
- **修复**：纯流体（ae2fc `ItemFluidPacket`）识别方式从"damage 查流体注册表"改为"按物品类名 + 读取 NBT"，与 ae2fc 实际编码一致；普通物品恢复为按普通物品显示数量
- **回归确认**：水/岩浆/蒸馏水等纯流体在 NEI 角标与提示中仍正确显示流体量（mB），桶/单元等容器仍按容器数量显示

### 3.1.1 — 修复汉化乱码（2026-08-16，作者 wztwzt）
- **修复**：中文汉化文件乱码全部恢复——按原始版本重建全部 67 条中文文本（按钮、映射、无线收发器/连接器、频道、提示等），全角标点与换行恢复正常，与英文版 key 完全一致

### 3.1.0 — 全量安全加固（2026-08-16，作者 wztwzt）
- **修复**：合成完成通知不再因假玩家/非 MP 玩家强转崩溃，长合成期间下线的玩家也不会触发通知 NPE
- **修复**：从 AE 网络提取物品不再丢物——先模拟计算、背包容量预检，放入失败时剩余物品归还网络
- **修复**：样板上传/撤回增加权限校验（仅持有对应安全权限的玩家可操作共享网络中的样板）
- **修复**：全部客户端到服务器网络包归队到服务端线程处理，不再在 IO 线程并发访问网络/容器，消除随机崩溃与状态不一致
- **修复**：NEI 配方页 AE 角标恢复正常（重写注入点适配 NEI 2.8.19）
- **修复**：Provider 列表全量扫描加限流，防恶意连发卡服；IO 端口倍率乘法防溢出
- **修复**：无线收发器动作仅允许操作自己当前打开的界面；合成下单/提取/样板交换全部异常兜底
- **清理**：移除 NEI 书签传输刷屏日志

### 3.0.2 — 流体直接显示（2026-08-15，作者 wztwzt）
- **修复**：桶/单元等流体容器在 NEI 角标和 tooltip 中改为直接显示流体本身的量（mB），不再显示容器个数
- **修复**：纯流体（水/岩浆/氢气等，ae2fc `ItemFluidPacket`）现在通过 damage 值识别流体，在 NEI 角标和 tooltip 中直接显示流体量
- **修复**：桶/单元等容器物品改为显示**容器本身在网络中的数量**（AE 里真的有该容器才显示），不再把容器当流体显示流体总量
- **改进**：流体 tooltip 追加流体本地名（如 `4.5P mB 蒸馏水`）
- **清理**：移除 4 条导致刷屏的流体缓存日志

### 3.0.1 — 崩溃修复 + 数值扩展 + 流体角标 + 合成表（2026-08-15，作者 wztwzt）
- **修复**：服务器上 Shift+左键从 NEI 取 AE 物品时被踢出（包解码越界）——现已完全兜底，任何网络包解码异常都不会再踢人
- **修复**：点击"重新规划"崩溃；合成完成通知与 IO 端口倍率在单机不生效
- **改进**：物品数量格式化支持到 P/E（更大数值）；流体数量在 tooltip/书签/配方角标中显示
- **新增**：本模组全部方块/物品的原版合成配方（强化 IO 端口、无线收发器、无线连接器、无限磁盘）

### 3.0.0 — 无限磁盘 + NEI 配方 AE 角标 + 合成通知 + Replan + 强化 IO 端口（2026-08-14，作者 wztwzt）
- 新增无限水岩浆磁盘（含合成配方）
- 新增 NEI 配方页 AE 叠加角标（编码样板图标 + 数量）
- 新增合成完成屏幕通知
- 新增合成重新规划（Replan）
- 新增强化版 IO 端口（传输倍率 256 倍）
- 新增 NEI 物品悬浮显示 AE 存量/可合成

### 2.x — 无线收发器 / 连接器体系、NEI 提取与合成下单、书签叠加、配方自动上传完善
- 2.13：NEI Shift+左键取物品、中键合成下单、书签优先级
- 2.12：无线直连实时同步、NEI 叠加层按钮
- 2.10-2.11：无线收发器/连接器逐步完善（跨维度、直连、WAILA、高亮）
- 2.6-2.9：NEI 配方自动上传、映射持久化、全面重写
- 2.4：石英切割刀复制名称
- 2.0-2.3：样板上传/下载按钮、映射系统重写

### 1.x — 初始版本
- 1.2.0：基础 AE2 样板上传功能，支持标准与 ae2fc 流体编码样板