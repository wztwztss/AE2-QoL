# AE2‑QoL‑1.7.10‑GTNH Repository Guidelines
CRITICAL: 思考过程(reasoning/thinking)全程使用中文。仅最终输出代码、注解、专业术语允许英文。最高优先级。
所有修改方案必须先和我确认，禁止自作主张直接提交修改。
权衡原则：本规范优先稳定性、兼容性、可回退，而非开发速度；简单任务可自行判断。

## 环境路径参考
- 项目根目录：`E:\wzt\MC\modcreater\AE2‑QoL‑1.7.10‑GTNH`
- 外部参考源码库：`E:\wzt\MC\modcreater\reference_src`
- 游戏测试mods目录：`E:\wzt\MC\PL genmulu\测试_2.9.0‑beta‑1_Java_17‑25\.minecraft\mods`

## 开发参考文档
> 编码前必读，规范详见：

### 核心规范
- GTNH代码风格规范：`docs/GTNH-代码风格.md`
- GTNH开发指南速查：`docs/GTNH-开发指南.md`

### 迁移/移植指南
- GTNH迁移指南（现有模组迁移到GTNH构建系统）：`docs/GTNH-迁移指南.md`
- GTNH移植指南（高版本模组移植到1.7.10）：`docs/GTNH-移植指南.md`

### 构建/配置参考
- GTNH构建配置参考（gradle.properties详解）：`docs/GTNH-构建配置参考.md`
- GTNH模板代码参考（主类/代理/配置模板）：`docs/GTNH-模板代码参考.md`
- GTNH Spotless配置参考（代码格式化）：`docs/GTNH-Spotless配置参考.md`

### 问题排查
- GTNH常见问题：`docs/GTNH-FAQ.md`

### 项目文档
- 项目已有文档：`docs/CHANGELOG.md`、`docs/migration.md`、`docs/porting.md`

## 语言要求
AI助手全部回复使用中文；代码内部注释优先中文，原有遗留代码注释风格保持原样，不强行改写旧注释。

---

## 1. 编码前先思考
**不要假设。不要隐藏困惑。暴露权衡。**
实现之前：
- 明确写出你的假设；对GTNH源码、Mixin行为、MC1.7.10/AE2行为不确定，直接提问，不要猜。
- 如果同一个需求存在多种实现路径，全部列出，对比利弊，不要默默选定某一个方案。
- 需要查阅其他模组参考代码时，优先查找 `E:\wzt\MC\modcreater\reference_src`。
- 主动提出更简单可行的方案；对不合理需求主动质疑。
- 信息不足就停止，明确写出哪里存在疑问，等待确认再动手。

> AE2‑QoL专项提醒：
> - 修改Mixin、AE2覆盖逻辑、配方逻辑，确认目标版本：GTNH 2.9.0‑beta‑1，MC1.7.10
> - 确认改动是否影响存档，是否和其他AE2附属模组冲突。

## 2. 简洁优先
**用最少代码解决问题，不写多余代码。**
- 不增加需求以外的功能。
- 不强行做通用抽象：模组中很多Mixin、补丁、特殊兼容代码只会使用一次，允许一次性专用代码，不强制抽取公共抽象。
- 不添加未要求的可配置项、灵活性。
- 不处理现实中几乎不可能触发的边界异常；Minecraft游戏本身会处理部分异常。
- 代码能精简就精简，拒绝过度封装。

自检：高级GTNH/AE2开发者看这段代码，会不会觉得过度复杂？如果是，进行简化。

## 3. 精准修改
**只改动必须修改的部分，清理自己引入的垃圾代码。**
编辑已有源码时：
- 禁止顺便"优化"相邻无关代码、注释、格式化，即使风格不好看。
- 不要重构没有问题的遗留旧代码。
- 严格跟随现有代码风格：缩进、命名、注释格式，不要全局美化。详见 `docs/GTNH-代码风格.md`。
- 如果看到原本就存在的死代码、废弃Mixin，只做标记提醒，**不要直接删除**。

修改产生无用代码时：
- 删除本次修改导致不再使用的导入、局部变量、私有函数。
- 原本就存在的死代码，除非明确要求，否则不动。

检验标准：每一行改动，都可以直接追溯到本次需求。

## 4. 目标驱动执行
**定义可验证成功标准，循环直到达成目标。**
把任务拆解成可验证目标：
- "修复崩溃bug" → 复现崩溃，修改代码，编译，将jar放入测试mods目录，运行游戏确认不再崩溃，检查无新警告、无Mixin冲突。
- "新增/修改AE2机器行为" → 修改代码，构建mod，替换测试环境jar，游戏内加载世界，GUI验证行为。
- "修改Mixin注入" → 确认注入点，构建mod，替换测试环境jar，启动游戏，检查mixins日志，确认注入成功，无破坏原有逻辑。

> 项目注意：很难写传统JUnit单元测试，**验证手段优先为游戏内运行、查看log、mixins日志、崩溃报告、Ingame调试**。

多步骤任务，输出简短计划模板：
[步骤描述] → 验证：[编译 / 游戏 /log/ 崩溃日志的检查项]
[步骤描述] → 验证：[编译 / 游戏 /log/ 崩溃日志的检查项]
[步骤描述] → 验证：[编译 / 游戏 /log/ 崩溃日志的检查项]

## 5. 文档驱动开发
> 复用项目已有的`docs/`目录，不新建文件夹。已有文件：`CHANGELOG.md`、`migration.md`、`porting.md`

### 5.1 修改代码前：阅读文档
1. 查阅 `docs/MOD_MAP.md`：定位功能对应的Java、Mixin类路径。
2. 阅读对应功能文档，理解机器逻辑、注入点、历史补丁、移植说明。
3. 阅读对应源码、mixin配置文件；需要参考其他模组代码，查阅外部库`E:\wzt\MC\modcreater\reference_src`。
4. 输出修改方案，确认后再编码。

### 5.2 修改完成后强制更新文档
代码完成，文档没有更新 = 任务未完成。

| 代码变更 | 需要更新文档 |
|---|---|
| 新增/删除/重写Java、Mixin类 | `docs/MOD_MAP.md` |
| 修改Mixin注入点、注入目标 | `docs/mixin_notes.md` |
| 修改模组行为、AE2逻辑、配方 | 对应功能文档；重大变更同步更新根目录`CHANGELOG.md` |
| 移植、版本迁移相关改动 | `docs/migration.md` / `docs/porting.md` |

> 根目录`CHANGELOG.md`面向使用者，记录对外可见变更。内部重构、注释微调不写入。

### 5.3 文档质量标准
- 文件路径精确到 `*.java` / `*.mixins.json`，使用**项目根目录相对路径**；外部参考库写完整绝对路径。
- 文档重点记录**为什么这么改（Why）**，而不是复述代码做了什么。
- 每条修改记录：版本号、时间戳、变更内容；版本历史全部交给Git管理，文档不重复存放完整历史。
- 文档是指南，不要复制粘贴大段源码。

### 5.4 文档过期识别
文档写的文件路径不存在、Mixin目标对不上、逻辑描述和源码不一致，判定文档过期。**优先修复文档，再修改代码。**

---

## 6. AE2‑QoL‑GTNH 附加强制规则
> 本项目绑定GitHub，所有版本历史、备份全部由Git完成，**不做本地文件备份**。

### 6.1 文件操作权限约束
1. 文件增删改操作，**仅限在项目根目录 `E:\wzt\MC\modcreater\AE2‑QoL‑1.7.10‑GTNH` 内部执行**。临时文件、调试文件全部放在此目录内。
2. 项目以外的其他文件夹，仅允许读取访问、调用工具读取内容；**禁止在外部文件夹执行新建、修改、删除文件操作**。
3. 确有必要操作项目外文件时，必须向你发起请求，获得许可之后再执行。
4. 例外：测试整合包的mods目录更新模组jar，属于允许场景，无需额外权限申请。
5. GitHub上传提交时，只上传模组项目相关文件；严禁上传无关文件、本地临时文件、隐私文件，严格遵守`.gitignore`配置。

### 6.2 Git版本约束
1. 所有改动必须提交git，推送到GitHub；回退使用git命令。
2. AI输出结构化总结中给出对应git操作：`git revert <commitHash>` / `git checkout <commitHash> -- 文件路径`。
3. commit粒度：一个功能/一个bug修复一个commit，禁止一次提交大量无关修改。
4. **代码变更、文档变更、版本号变更放在同一个commit提交，禁止拆分多次提交。**
5. 禁止提交构建产物：`build/`、`.gradle`、run目录，确认`.gitignore`生效。
6. 尽量不直接push main主分支，优先新建特性分支，确认完成后合并。

### 6.3 Mixin专项约束
1. 修改Mixin必须核对目标类、方法名、SRG签名，适配GTNH 2.9.0‑beta‑1 / MC1.7.10。
2. 修改完成后Gradle构建jar，放入测试mods目录，启动游戏，必须查看`mixin.log`，无注入失败、无警告。
3. 尽量避免大规模`@Overwrite`；必须使用时，在`docs/mixin_notes.md`写明风险。
4. Mixin target写错会静默失效，不崩溃但功能无效，必须以mixin.log为准。
5. AE2部分类被GTNH修改，不能直接照搬原版AE2的Mixin。

### 6.4 编译与验证固定流程
1. 修改完成执行Gradle构建，编译无报错，产出jar包。常用命令详见 `docs/GTNH-开发指南.md`。
2. 将`build/libs`输出jar复制替换：`E:\wzt\MC\PL genmulu\测试_2.9.0‑beta‑1_Java_17‑25\.minecraft\mods`，**删除旧版本jar，禁止新旧jar共存**。
3. 两层测试：新建空存档测试基础功能；旧存档测试存档兼容性。
4. IDE run仅用于快速调试，**最终验证必须使用build/libs下jar在真实mods环境测试**。
5. 出现崩溃优先保存：`fml‑client‑latest.log`、`mixin.log`、crash报告，不要重启覆盖日志。

### 6.5 存档兼容性硬性规则
1. 修改方块实体NBT存储逻辑，必须评估存档兼容性，必要时编写NBT迁移逻辑。
2. 破坏性存档改动，必须在CHANGELOG、README醒目提示，写入migration.md。
3. 优先新增NBT字段，尽量避免修改、删除已有NBT字段。

### 6.6 版本迭代、日志与README维护（模组发布更新强制）
> 每次模组迭代更新，四件事必须同步完成，放在同一个commit：
1. **迭代版本号**：修改`gradle.properties`以及mods.toml中的模组版本号，遵循语义化版本 `主版本.次版本.补丁版本`。
    - 补丁版本：bug修复、小优化；
    - 次版本：新增功能，向下兼容；
    - 主版本：破坏性改动、存档不兼容大更新。
2. **更新根目录 CHANGELOG.md**：写明新版本号、更新日期、新增功能、修复bug、已知问题、破坏性变更提示。
3. **更新根目录 README.md（GitHub对外展示）**
    - 维护模组简介、主要功能列表；
    - 详细使用方法、操作说明；
    - 支持的GTNH版本、依赖、安装方法；
    - 存档兼容性提示、已知问题；
    - 版本更新提示。
4. 同步核对README.en.md英文版本，关键变更同步更新。

> 小bug修复补丁版本，README可以只更新版本号与兼容性提示；新增功能、行为改动必须同步更新功能说明与使用教程。

### 6.7 任务输出结构化总结模板（每次任务结束强制输出）
版本：GTNH 2.9.0‑beta‑1 | MC1.7.10
时间戳：yyyy‑MM‑dd
修改文件：[src/xxx.java, src/main/resources/mixins.xxx.json, gradle.properties, CHANGELOG.md, README.md]
Git 回退方案：git revert <commitHash> 或者 git checkout <commitHash> -- 文件路径
测试操作：构建 jar，替换至 E:\wzt\MC\PL genmulu\ 测试_2.9.0‑beta‑1_Java_17‑25.minecraft\mods
验证结果：编译通过，游戏运行，mixins 日志无报错
版本迭代：旧版本 → 新版本
问题出现的原因：XXX
解决的方法：XXX
需要测试的内容：：XXX
接下来的任务：XXX

### 6.8 附加实操注意事项
1. `E:\wzt\MC\modcreater\reference_src`仅用于阅读参考，禁止直接复制源码进入本项目src目录。
2. 测试必须清理mods目录旧版本jar，避免多jar并存引发未知冲突。
3. IDE run环境仅作快速调试，最终验证必须使用 `build/libs`产出jar放入测试`.minecraft/mods`。
4. Mixin修改后必须查看mixin.log，警告也需要处理，不能仅以游戏是否崩溃作为判断标准。
5. 修改方块实体NBT存储，优先评估存档兼容性，破坏性改动必须做迁移并标记。

---

## 7. 开发前快速检查清单
> 开始编码前逐项确认：

### 7.1 编码前
- [ ] 阅读 `docs/GTNH-代码风格.md`（命名、注释、switch语法、括号规则）
- [ ] 阅读 `docs/GTNH-开发指南.md`（构建命令、调试方法）
- [ ] 如需迁移/移植，阅读 `docs/GTNH-迁移指南.md` 或 `docs/GTNH-移植指南.md`
- [ ] 确认目标版本：GTNH 2.9.0‑beta‑1 / MC1.7.10
- [ ] 查阅 `docs/MOD_MAP.md` 定位目标类路径
- [ ] 输出修改方案，等待确认

### 7.2 编码中
- [ ] 遵循GTNH命名约定（类名名词、方法动词、接口不用I前缀）
- [ ] 不注释掉代码，直接删除（Git历史保留）
- [ ] Mixin目标类、方法名、SRG签名已核对
- [ ] 不"顺便"优化无关代码

### 7.3 提交前
- [ ] `./gradlew build` 编译通过
- [ ] jar已替换至测试mods目录，删除旧版本
- [ ] 新建空存档测试基础功能
- [ ] 旧存档测试兼容性
- [ ] `mixin.log` 无报错、无警告
- [ ] 文档已更新（MOD_MAP.md / CHANGELOG.md / README.md）
- [ ] 版本号已迭代（如有功能变更）
